services:
  - type: web
    name: calendar-booking
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /event-types
    envVars:
      - key: NODE_ENV
        value: production
